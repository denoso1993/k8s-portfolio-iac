apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubectl-proxy
  namespace: default
  labels:
    app: kubectl-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubectl-proxy
  template:
    metadata:
      labels:
        app: kubectl-proxy
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
      - name: kubectl-proxy
        image: bitnami/kubectl@sha256:08afc880eea24f36572644ccae85fb3e573a6ff1b7161135a3ae9a5eab222df2
        imagePullPolicy: IfNotPresent
        command:
        - kubectl
        - proxy
        - --port=8001
        - --address=0.0.0.0
        - --accept-hosts=.*
        - --accept-paths=^/api/v1/(pods|nodes|services|namespaces)(/|$)
        ports:
        - containerPort: 8001
          name: http
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 100m
            memory: 128Mi
        livenessProbe:
          httpGet:
            path: /api/v1/nodes
            port: 8001
          initialDelaySeconds: 10
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/v1/nodes
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: kubectl-proxy
  namespace: default
spec:
  selector:
    app: kubectl-proxy
  ports:
  - port: 8001
    targetPort: 8001
    name: http
