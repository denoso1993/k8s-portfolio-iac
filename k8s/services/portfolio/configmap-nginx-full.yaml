apiVersion: v1
data:
  nginx.conf: |-
    worker_processes auto;
    error_log /var/log/nginx/error.log notice;
    pid /tmp/nginx.pid;
    events {
      worker_connections 2048;
    }
    http {
      include /etc/nginx/mime.types;
      default_type application/octet-stream;
      gzip on;
      gzip_types text/html text/css application/javascript application/json;
      gzip_min_length 256;
      gzip_vary on;
      add_header X-Content-Type-Options nosniff always;
      add_header Referrer-Policy no-referrer-when-downgrade always;
      add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
      server {
        listen 8080;
        root /usr/share/nginx/html;
        index index.html;
        location / {
          try_files $uri $uri/ /index.html;
          expires 1h;
          add_header Cache-Control "public, must-revalidate";
          add_header X-Frame-Options DENY always;
        }
        location /k8s/ {
          proxy_pass http://172.19.105.82:8002/;
          proxy_set_header Host localhost;
          proxy_set_header X-Real-IP $remote_addr;
        }
        location /grafana/ {
          proxy_pass http://grafana.monitoring.svc.cluster.local/;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
        }
      }
    }
kind: ConfigMap
metadata:
  creationTimestamp: "2026-06-11T18:50:39Z"
  name: nginx-full-config
  namespace: default
  resourceVersion: "7686"
  uid: 58e25b49-e7f4-466e-91b6-a5c24bc17ecc
